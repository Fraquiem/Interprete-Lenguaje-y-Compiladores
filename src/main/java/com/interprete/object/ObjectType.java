package com.interprete.object;

/**
 * Enum que define los tipos de objetos que pueden existir en runtime.
 */
public enum ObjectType {
    INTEGER,
    BOOLEAN,
    NULL,
    STRING,
    FUNCTION,
    RETURN_VALUE
}
